# Production backend (Render). /api is appended automatically if omitted.
# VITE_API_URL=https://aura-learning-management-system.onrender.com

# Local development: leave unset to use Vite proxy (/api -> http://localhost:5000).
